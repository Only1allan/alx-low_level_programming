# include "main.h"
/**
 * mul - a function that multiplys two intergers
 * @a: an argument of the mul function
 * @b: an argument of the mul function
 * Return: multiplication of the two ints
 */
int mul(int a, int b)
{
	return (a * b);
}
