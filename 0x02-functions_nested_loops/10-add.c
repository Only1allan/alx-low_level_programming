# include "main.h"
/**
 * add - a function that takes two integers and rerturns their result
 * @x: arguments of the add function
 * @y: arguments of the add function
 * Return: void
 */
int add(int x, int y)
{
	return (x + y);
}
